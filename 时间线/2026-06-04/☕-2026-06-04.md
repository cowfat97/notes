# ☕ 2026-06-04

- 地点：包头
- 天气：☀️晴

---

## 💆‍♂️ 状态

- 精力：⚡⚡⚡☆☆ (3/5)
- 心情：😐一般
- 今日小确幸：

---

## 🧠 学习

- 博客全面重构：
  - 目录收敛 —— Java/LLM/408/中间件/数据库/软考 归入 `文章/`，顶层从 17 个目录减到 9 个
  - CSS 组件化 —— 12 个页面内联 `<style>` 提取到 `_components/*.css`（+285行，-900行）
  - 博客统一 —— `posts/` `blog/` `公众号.md` `.authors.yml` 全部归入 `blog-notes/`
  - 脚本归位 —— `scripts/` 删除，Python 脚本移入 `_components/`
  - 时间线修复 —— 去掉 `hide: navigation`，恢复左边栏导航；样式改为滚动容器
  - mkdocs build 测试通过，`blog_dir: "blog-notes"`
- 微信读书数据同步
- Obsidian vault 健康检查通过

## 💼 工作

无

## 📝 想法

目录重构后仓库结构清晰很多，后续加新内容不容易散乱。CSS 集中管理后改样式只需改一个文件。
