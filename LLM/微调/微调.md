# 模型微调 Fine-Tuning

## 核心概念

微调是在预训练模型基础上，用特定领域数据继续训练，让模型适应下游任务。

**本质**：预训练学通用知识 → 微调学特定任务。

## 微调 vs RAG vs Prompt Engineering

| 维度 | Prompt Engineering | RAG | 微调 |
|------|-------------------|-----|------|
| 成本 | 最低 | 低 | 高（GPU + 数据） |
| 知识更新 | 手动 | 实时 | 需重新训练 |
| 定制深度 | 浅 | 中 | 深 |
| 适用场景 | 简单任务 | 知识密集型 | 风格/格式/行为改变 |
| 数据需求 | 几条示例 | 文档库 | 几百到几万条 |

**选型原则**：
- 先试 prompt → 不够用试 RAG → 还不够才微调
- 格式输出固定、风格需要统一 → 微调优先
- 知识频繁更新 → RAG 优先
- 两者不互斥，可以 RAG + 微调一起用

## 微调方法分类

### 全量微调 Full Fine-Tuning
- 更新所有参数
- 效果最好，显存开销最大
- 7B 模型全量微调 ≈ 112GB 显存（AdamW 优化器下约每 1B 参数需 16GB）

### 参数高效微调 PEFT

**LoRA**（Low-Rank Adaptation）
- 冻结原有权重，插入低秩矩阵只训练那部分
- 训练参数减少 99%+
- 7B 模型 LoRA 微调 ≈ 16GB 显存
- 核心思想：ΔW 是低秩的，W' = W + B·A

**QLoRA**
- LoRA + 4-bit 量化
- 7B 模型 QLoRA ≈ 8GB 显存，单卡 3090 就能跑
- 牺牲少量精度，大幅降低门槛

**其他方法**：
- Adapter：插入小型网络层
- Prefix Tuning：学习可训练的前缀向量
- Prompt Tuning：在输入前加可学习的软 prompt
- IA³：比 LoRA 更省，只学习 rescaling 向量

## 微调流程

```
1. 明确任务
   ├── 分类？生成？NER？指令跟随？
   └── 定义输入输出格式

2. 准备数据
   ├── 收集 → 清洗 → 格式化
   ├── 格式通常：{"instruction": "...", "input": "...", "output": "..."}
   ├── 数据量：分类几百条，生成几千条
   └── 切分 train/val/test

3. 选基座模型 + 方法
   ├── 中文：Qwen、GLM、DeepSeek
   ├── 英文：Llama、Mistral、Gemma
   └── 方法：一般用 QLoRA 起步

4. 训练
   ├── 框架：Transformers + PEFT + bitsandbytes
   ├── 关键超参：lr(1e-4~5e-5)、epochs(1~5)、batch_size
   └── 监控 loss，防止过拟合

5. 评估
   ├── 验证集上的指标
   └── 实际场景手动测试

6. 部署
   ├── 合并 LoRA 权重 → 导出完整模型
   ├── 格式转换 GGUF/Ollama/vLLM
   └── 推理部署
```

## 数据格式

### Alpaca 格式（指令微调最常用）

```json
{
  "instruction": "将以下句子翻译成英文",
  "input": "今天天气很好",
  "output": "The weather is nice today."
}
```

### ShareGPT 格式（多轮对话）

```json
{
  "conversations": [
    {"from": "human", "value": "什么是微调？"},
    {"from": "gpt", "value": "微调是..."},
    {"from": "human", "value": "和预训练有什么区别？"},
    {"from": "gpt", "value": "预训练学通用知识，微调学特定任务。"}
  ]
}
```

## 代码示例（QLoRA 微调 Qwen2.5-7B）

```python
from transformers import (
    AutoModelForCausalLM, AutoTokenizer, TrainingArguments,
    BitsAndBytesConfig
)
from peft import LoraConfig, get_peft_model, TaskType
from datasets import Dataset
import torch

# 1. 加载量化模型
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.bfloat16,
)

model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2.5-7B-Instruct",
    quantization_config=bnb_config,
    device_map="auto",
    trust_remote_code=True,
)

# 2. 配置 LoRA
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=8,                    # 秩，8~64，越大效果越好但参数越多
    lora_alpha=32,          # 缩放因子，通常 r 的 2~4 倍
    lora_dropout=0.1,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],  # 目标层
)

model = get_peft_model(model, lora_config)
model.print_trainable_parameters()  # 打印可训练参数量

# 3. 准备数据
dataset = Dataset.from_json("training_data.json")

def format_example(example):
    text = f"<|im_start|>user\n{example['instruction']}\n{example['input']}<|im_end|>\n"
    text += f"<|im_start|>assistant\n{example['output']}<|im_end|>"
    return {"text": text}

dataset = dataset.map(format_example)
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-7B-Instruct")

def tokenize(examples):
    return tokenizer(
        examples["text"], truncation=True, padding="max_length", max_length=1024
    )

tokenized_dataset = dataset.map(tokenize, batched=True)

# 4. 训练
training_args = TrainingArguments(
    output_dir="./qwen-lora-finetuned",
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    num_train_epochs=3,
    logging_steps=10,
    save_strategy="epoch",
    bf16=True,
)

from transformers import Trainer

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset,
)
trainer.train()

# 5. 保存及合并
model.save_pretrained("./lora-adapter")
tokenizer.save_pretrained("./lora-adapter")

# 合并权重（部署用）
merged_model = model.merge_and_unload()
merged_model.save_pretrained("./merged-model")
```

## 常见问题

### 过拟合
- 现象：训练 loss 降但验证 loss 升
- 解法：减少 epochs、增大 dropout、减小 lora_r、加更多数据

### 灾难性遗忘
- 现象：微调后原来会的东西忘了
- 解法：混合部分通用数据、降低学习率、减少训练量

### 数据质量差
- 微调不是"喂得多就好"，脏数据比没数据更糟糕
- 手工检查几十条样本，确保格式统一、答案正确

## 工具生态

| 工具 | 用途 |
|------|------|
| Transformers + PEFT | 基础框架 |
| bitsandbytes | 4/8-bit 量化 |
| Llama-Factory | 一键微调，支持多种方法，推荐新手 |
| Axolotl | 配置驱动的微调框架 |
| Unsloth | 极致显存优化，提速 2-5x |
| ModelScope (swift) | 阿里系模型微调首选 |

## 实战建议

1. 先用 Llama-Factory 的 Web UI 跑通流程，理解每个环节
2. 数据准备是最耗时的一步，做好数据标注规范
3. QLoRA 是性价比最高的起步方案
4. 训练完先用 10 条真实 case 手动测试，别只信 loss
