---
date: 2026-06-04
---
# RAG 文档处理

## What — 是什么

文档处理是 RAG 的第一道关卡。把任意格式的文件（PDF、Word、Markdown、PPT）转成结构化的文本块，供下游向量化和检索使用。

核心四步：

```text
任意格式文件(.pdf/.docx/.md/.pptx)
  → 布局识别（区分标题/正文/表格/图片）
  → 元素提取（表格保留行列，图片走 OCR，文字保留层级）
  → 转为 Markdown（统一结构格式）
  → 分割（结构感知切分 + 父子块）
```

## Why — 为什么

文档处理不好，Embedding 再准、Reranker 再强都白费。

- PDF 没有 `<h1>` `<table>` 标签，只有"在 x,y 坐标画了某段文字"。不识别布局，标题跟正文混在一起，表格变成散落文字。
- OCR 选错引擎（Tesseract 做中文），"线程池"变成"线裎池"，后续全链路污染。
- 切分时按固定长度拦腰切断，标题和正文分到两个 chunk，语义碎片化。

**一句话**：文档解析质量 = 检索质量的上限。解析丢了 30% 的结构，后面的 Embedding 再好也只能用 70% 的信息。

## How — 怎么做

### 1. 布局识别

`unstructured` 负责找出页面上哪块是标题、哪块是正文、哪块是表格。

| strategy | 原理 | 速度 | 精度 |
|----------|------|------|------|
| `auto` | 有文字层直接提取，没有才 OCR | 快 | 中 |
| `hi_res` | YOLOX 目标检测模型先识布局 | 慢 | 高 |
| `ocr_only` | 整页 OCR，跳过布局模型 | 慢 | 低 |

`hi_res` 技术原理：YOLOX CSPDarkNet 提取特征 → FPN 多尺度融合 → 预测每个 bbox 坐标 + 类别。模型训练时学了：14pt 加粗居中 = 标题，10pt 对齐多行 = 正文。

```python
from unstructured.partition.auto import partition

elements = partition(filename="doc.pdf", strategy="hi_res")
for el in elements:
    print(el.category)  # Title / NarrativeText / Table / Image / ListItem
```

### 2. 元素提取（图片 + 表格 + OCR）

#### 2.1 OCR 的本质困难

OCR 解决一个问题：**从像素网格还原离散符号序列**。这是一个病态问题——同一个文字可以有不同的字体、大小、旋转角度、光照条件，但最终都要映射到同一个 Unicode 码点。

中文 OCR 比英文难一个数量级：

| | 英文 | 中文 |
|------|------|------|
| 字符集 | 26 字母 + 数字 | 常用 6,763 字（GB2312），全量 20,000+ |
| 字符形态 | 线性排列，单个字母独立 | 方块结构，笔画交织（"已"和"己"差 1 像素） |
| OCR 策略 | 逐字母识别 + 拼词 | 必须整字识别，无法拆笔画 |
| 发动机 | 词典纠错可修复 90% 错误 | 不认识的字就是玄学，词典覆盖率远低于英文 |

**为什么会认错**：OCR 面对的是一个从高维像素空间到低维符号空间的压缩映射。例如 "未" 和 "末"——两个字的区别仅仅是两条横线的长短比例。CNN 的一个 max-pooling 层就可能把关键像素抹掉，导致完全无法区分。

#### 2.2 PaddleOCR 三阶段管线

PaddleOCR 把问题拆成三个子任务，每个各用独立模型：

```text
输入图片 (H×W×3 像素)
  → DB 检测（圈出每块文字的 bbox 坐标）
  → Cls 方向分类（判断 0°/90°/180°/270°）  
  → CRNN 识别（bbox 内像素 → 文字序列）
```

**为什么拆成三阶段而不是端到端**：检测和识别对分辨率要求不同。检测需要高分辨率定位边界（但不需要知道是什么字），识别需要低分辨率 Semantic 特征（但需要理解上下文）。一个模型同时做两件事会导致两个任务都做不好。

#### 2.3 DB（Differentiable Binarization）— 文字检测

传统方法用固定阈值二值化——像素值大于 127 的算前景，否则算背景。对于不均匀光照（阴影、闪光灯），固定阈值直接失效。

DB 的核心思想：**让二值化步骤可导**，从而融入 CNN 训练。

```
CNN backbone → 特征图 F
  → F 通过 1×1 卷积预测 probability map P（每个像素是文字的概率）
  → F 同时预测 threshold map T（每个像素的二值化阈值应该是多少）
  → B = 1 / (1 + e^(-k × (P - T)))  ← 可微二值化
  → 从 B 中提取连通域 → 收缩 → 膨胀 → 得到最终 bbox
```

关键公式——**可微二值化**：

```
B̂(i,j) = 1 / (1 + e^(−k × (P(i,j) − T(i,j))))
```

k 是放大系数（通常取 50）。当 k 很大时，这个函数的图像近似于阶梯函数，但梯度仍然存在。训练时网络通过调整 P 和 T 的预测值来最小化 B 与真实标注之间的差异。

**为什么比固定阈值好**：

- 固定阈值：光照暗的区域全被判为背景，漏检
- DB：每个像素自适应阈值——阴影处阈值自动降低，亮处阈值自动提高
- 空洞效果：DB 能生成更紧致的文字边界框，相邻文字不会粘连，后续识别阶段每个框里正好包含一个完整文字行

#### 2.4 Cls — 方向分类

一个轻量 CNN（MobileNetV3），只做四分类：0°、90°、180°、270°。因为较正后文字方向固定，不需要角度回归。不展开。

#### 2.5 CRNN + CTC — 文字识别

这是三道坎。

##### 坎一：不定长输入 → 定长输出

CNN 处理的是固定尺寸的图片（224×224），但文字 bbox 的长宽比变化极大：一行 3 个字和一行 30 个字的 bbox 形状完全不同。直接 resize 会扭曲文字形状。

CRNN 的解法：**CNN 提取特征 → 沿宽度方向切分成时间步 → RNN 处理序列**。resize 到固定高度（32px），宽度自由——长行产生更多时间步，短行产生更少时间步。

```text
bbox 图片 [32 × W]
  → CNN backbone（ResNet/VGG）→ 特征图 [1 × W/4 × 512]
  → 沿宽度方向切分 → 序列 seq_len=W/4 每步 dim=512
  → BiLSTM(512 → 256) 双向捕捉上下文
  → 每个时间步 softmax(256 → num_classes) 输出字符集概率分布
```

##### 坎二：特征帧和字符不对齐

这是 OCR 的核心困难。看这个例子：

```
输入: "你好" (2 个字符)
CNN 提取后: 8 个时间步 (W/4 = 8)

理想: [你][你][你][你][你][好][好][好]  ← 每个字符占连续几帧
现实: [你][你][你][好][你][好][好][好]  ← 帧与帧之间不稳定，"你"和"好"混在一起
正常: [你][你][你][你][好][好][好][好]  ← 也被认为 OK

但以下一定错误:
[你][好][你][好][你][好][你][好]  ← 野字符交替，两个都不是正常文字
```

CRNN 的 8 帧输出里，哪 4 帧对应"你"、哪 4 帧对应"好"——没有标注。中文也没有空格来分割。CTC（Connectionist Temporal Classification）就是来解决这个对齐问题的。

##### 坎三：CTC 的对齐策略

CTC 引入一个特殊标记 ε（blank，空白），含义是"这一帧没有输出任何字符"。

**路径到最终序列的折叠规则**：

1. 去重：连续相同的字符合并成一个
2. 去空：去掉所有 ε

```
路径: [你][你][ε][你][好][ε][好][好]
第 1 步去重: [你][ε][你][好][ε][好]
第 2 步去空: [你][好]
→ 就是正确答案 ✓
```

CTC 的优化目标：**最大化所有能折叠成正确标注的路径的概率之和**。即使不确定具体哪一帧对应哪个字，只要存在某个合理路径能产生正确答案，CTC 就给高分。这从根上解决了对齐标注缺失的问题。

**训练和推理的不同**：

- 训练：前向-后向算法（类似 HMM 的 α-β 递归），算所有合法路径概率和，梯度反向传播更新 CNN+RNN 参数
- 推理：贪心解码取每帧最大概率字符 → 去重去空 → 输出。或前缀束搜索（prefix beam search）保留 top-k 候选

**CTC 的局限性**：假设每帧输出独立（条件独立假设）。这意味着如果"已"和"己"的视觉特征很接近，CTC 无法使用语言上下文区分它们——因为语言信息必须跨帧传递，而 CTC 的独立假设禁止了这一点。所以在 PaddleOCR 中，CTC 解码后通常还会接一个语言模型做后纠正（可以用 KenLM 或拼音纠错）。

#### 2.6 PaddleOCR vs Tesseract 架构对比

| | Tesseract | PaddleOCR |
|------|-----------|-----------|
| 检测 | 传统 CV（连通域分析 + 固定阈值二值化） | DB CNN（可微二值化） |
| 识别 | LSTM + 字符切割（需显式分割字符边界） | CRNN + CTC（端到端，无需字符级标注） |
| 中文支持 | 通过 LSTM 扩展，不是原生设计 | 原生中文训练，识别字符集 6K+ |
| 抗干扰 | 弱——阴影/倾斜/模糊直接失败 | 强——DB 自适应阈值 + 数据增强 |
| 训练数据 | 英文为主 | 中文 + 英文混合，多场景覆盖 |

**结论**：Tesseract 设计时假设输入是扫描仪扫的干净 A4 纸，PaddleOCR 设计时假设输入可能是手机拍的歪斜发票。RAG 场景的文档来源不可控，必须 PaddleOCR。

#### 2.7 场景调参

| 场景 | `det_db_thresh` | `use_angle_cls` | 原因 |
|------|----------------|-----------------|------|
| 普通文档 | 0.3 | 可关 | 文字清晰，方向确定 |
| 银行申请件 | 0.2 | 开 | 扫描件颜色淡，需降低检测阈值避免漏字；方向因扫描仪而异 |
| 手机拍照 | 0.15 | 必开 | 光照不均匀，角度随机，DB 阈值必须降到最低 |

**unstructured + PaddleOCR 连接**：`unstructured` 管"哪块是文字"，PaddleOCR 管"那块是什么字"。装 `unstructured[paddleocr]` 后内部自动对接。

```python
from unstructured.partition.pdf import partition_pdf
from paddleocr import PaddleOCR
from unstructured_paddle import PaddleOCRAdapter

# 精细控制场景
ocr = PaddleOCR(lang="ch", use_angle_cls=True, use_gpu=False, det_db_thresh=0.2)
elements = partition_pdf("doc.pdf", strategy="ocr_only", 
                         ocr=PaddleOCRAdapter(ocr=ocr))
```

**误差传播**：OCR 这里每错一个字（"未"→"末"），后续 embedding 向量就偏了、检索就召回了错的上下文、LLM 就基于错误片段回答了——而用户只会骂 LLM 胡说八道，根本不知道是第一步 OCR 埋的雷。

### 3. 转为 Markdown

不同格式经过前两步处理后，统一转为 Markdown——保留 `##` 标题、`|表格|`、` ```code``` `。

```python
def elements_to_markdown(elements):
    md_parts = []
    for el in elements:
        if el.category == "Title":
            md_parts.append(f"## {el.text}\n")
        elif el.category == "Table":
            md_parts.append(html_to_md(el.metadata.text_as_html))
        elif el.category == "Image":
            md_parts.append(f"[图片: {ocr(el.image)}]\n")
        elif el.category == "ListItem":
            md_parts.append(f"- {el.text}\n")
        else:
            md_parts.append(f"{el.text}\n\n")
    return "\n".join(md_parts)
```

**为什么是 Markdown**：LLM 原生理解，MarkdownTextSplitter 原生理解（不横切表格和代码块），跟笔记格式一致。

### 4. 分割

**核心矛盾**：检索要小块（精准匹配 query），LLM 要大块（完整上下文）。

切分方式从差到好分五层。

#### 4.1 固定长度切分

```python
chunks = [text[i:i+300] for i in range(0, len(text), 300)]
```

三个问题：单词拦腰切断、标题被塞进前一个 chunk 的结尾、语义不相关的内容被硬粘在一起。**永远不要用。**

#### 4.2 标点边界切分

```python
from langchain_text_splitters import RecursiveCharacterTextSplitter

splitter = RecursiveCharacterTextSplitter(
    chunk_size=300, chunk_overlap=50,
    separators=["\n\n", "\n", "。", "，", " "]
)
```

`separators` 是优先级列表：先试段落边界，不行逐级下沉到句子、短句、最后空格。句子保持完整，不会在单词中间切断。**局限**：对所有文档一视同仁，不感知 Markdown 结构。

#### 4.3 Markdown 结构感知切分

```python
from langchain_text_splitters import MarkdownTextSplitter

splitter = MarkdownTextSplitter(chunk_size=1200, chunk_overlap=100)
```

| 结构 | 策略 |
|------|------|
| `##` / `###` 标题 | 优先在此处切——新标题 = 新主题 |
| 表格 | 绝对不切——行列结构跨行就废了 |
| 代码块 | 绝对不切——语法跨 chunk 全毁 |
| `- 列表项` | 列表项之间可以切 |
| 普通段落 | 按 `\n\n` → `\n` → `。` 优先级 |

#### 4.4 中文递归切分

课程自定义 `ChineseRecursiveTextSplitter`，继承 `RecursiveCharacterTextSplitter`，换中文标点优先级：

```python
separators = [
    "\n\n",           # 段落
    "\n",             # 换行
    "。|！|？",      # 中文句号
    "\.\s|\!\s|\?\s", # 英文句号
    "；|;\s",         # 分号
    "，|,\s"          # 逗号
]
```

原版没有中文标点，不加的话中文标点边界切分完全失效。

**按格式选择切分器**：

```python
is_markdown = (file_extension == ".md")
parent_splitter = MarkdownTextSplitter(chunk_size=1200) if is_markdown \
    else ChineseRecursiveTextSplitter(chunk_size=1200)
```

#### 4.5 父子块

**核心矛盾**的答案：子块搜，父块看。

```
父块 1200 token（给 LLM 看 = 完整章节上下文）
  ├─ 子块1 300 token ← 检索命中 → 返回整个父块
  ├─ 子块2 300 token
  ├─ 子块3 300 token
  └─ 子块4 300 token
```

每个子块携带父块信息：

```python
sub_chunk.metadata["parent_id"] = parent_id          # 指向父块
sub_chunk.metadata["parent_content"] = parent_doc.page_content  # 父块全文
sub_chunk.metadata["id"] = f"{parent_id}_child_{k}"  # 子块唯一 ID
```

检索链路：query 向量化 → 命中子块 → 不返回子块内容 → 返回 parent_content → LLM 看到完整上下文。

#### 4.6 辅助机制

**Overlap**：前后 chunk 重叠 100-150 token，避免关键信息恰好落在切断点。

**Metadata 拼接**：每个 chunk 拼上标题路径：

```
原始: "corePoolSize 控制核心线程数..."
增强: "Java并发 > 线程池 > 核心参数\n\ncorePoolSize 控制核心线程数..."
```

检索时"线程池"和"核心线程数"同时存在在向量中，命中率更高。

**chunk_size 取值**：父块 1000-1200 token，子块 200-300 token，overlap 100-150 token。

#### 4.7 完整切分流程

```python
def process_documents(directory_path):
    documents = load_documents_from_directory(directory_path)  # 1. 加载

    # 2. 准备切分器（通用 + Markdown 专用）
    parent_splitter = ChineseRecursiveTextSplitter(chunk_size=1200, chunk_overlap=100)
    child_splitter  = ChineseRecursiveTextSplitter(chunk_size=300,  chunk_overlap=50)
    md_parent_splitter = MarkdownTextSplitter(chunk_size=1200, chunk_overlap=100)
    md_child_splitter  = MarkdownTextSplitter(chunk_size=300,  chunk_overlap=50)

    child_chunks = []
    for i, doc in enumerate(documents):
        # 3. 按格式选切分器
        is_md = doc.metadata["file_path"].endswith(".md")
        p_splitter = md_parent_splitter if is_md else parent_splitter
        c_splitter = md_child_splitter  if is_md else child_splitter

        # 4. 父块切分 → 5. 子块切分 → 6. 子块继承父块信息
        parent_docs = p_splitter.split_documents([doc])
        for j, parent_doc in enumerate(parent_docs):
            parent_id = f"doc_{i}_parent_{j}"
            parent_doc.metadata["parent_id"] = parent_id
            parent_doc.metadata["parent_content"] = parent_doc.page_content

            sub_chunks = c_splitter.split_documents([parent_doc])
            for k, sub_chunk in enumerate(sub_chunks):
                sub_chunk.metadata.update({
                    "parent_id": parent_id,
                    "parent_content": parent_doc.page_content,
                    "id": f"{parent_id}_child_{k}"
                })
                child_chunks.append(sub_chunk)

    return child_chunks  # 存入 Milvus
```

#### 4.8 语义分割模型

`AliTextSplitter`（达摩院 BERT 文档语义分割模型）让模型判断"这个位置是不是语义边界"。

| 场景 | 是否需要 |
|------|---------|
| Markdown 笔记（有 `##` 标题） | 不需要，标题就是边界 |
| PDF OCR 纯文本 | **需要** |
| 合同、法规（格式被 OCR 吞掉） | **需要** |

笔记场景 `MarkdownTextSplitter` 已足够，银行文档场景语义分割模型有意义。

---

## 技术栈

| 环节 | 工具 |
|------|------|
| 布局识别 | `unstructured` + YOLOX |
| OCR | `PaddleOCR` (DB + CRNN) |
| 表格提取 | PP-TableStructure / `pdfplumber` |
| 格式统一 | 自拼装 + `markdownify` |
| 结构切分 | `MarkdownTextSplitter` + `ChineseRecursiveTextSplitter` |
| 父子块 | 代码逻辑 |
| 语义分割 | `AliTextSplitter`（可选） |
