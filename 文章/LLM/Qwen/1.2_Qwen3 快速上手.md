# Qwen3 快速上手

从零开始本地跑通 Qwen3：环境搭建 → 最小可运行代码。Mac 16GB 内存实测跑通 Qwen3-1.7B，本文所有输出均为实际运行结果。

## 1 环境准备

```bash
# 1. 用 uv 建独立环境（Python 3.11，不依赖 anaconda）
cd Qwen
uv venv

# 2. 安装依赖（requirements.txt）
uv pip install -r requirements.txt
```

```text
# requirements.txt
torch==2.14.0
transformers==5.16.1
accelerate==1.14.0
```

三个包的分工：torch 是计算框架（Mac 上走 MPS 加速），transformers 是模型库（AutoModelForCausalLM / AutoTokenizer 都来自它），accelerate 支持 `device_map="auto"` 自动设备分配。

首次运行会自动从 HuggingFace 下载权重到 `~/.cache/huggingface`。Qwen3 全家族（0.6B / 1.7B / 4B / 8B / 14B / 32B / 30B-A3B / 235B-A22B）均为 apache-2.0 开源，无需登录。

## 2 最小可运行代码

```python
from transformers import AutoModelForCausalLM, AutoTokenizer

model_name = "Qwen/Qwen3-1.7B"

# 加载模型和分词器
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype="auto",   # 精度自动选：优先用模型默认精度，兼顾内存
    device_map="auto"     # 设备自动分配：Mac 上走 MPS，没有就退回 CPU
)
tokenizer = AutoTokenizer.from_pretrained(model_name)

# 构造一条用户消息
messages = [
    {"role": "user", "content": "请用中文介绍一下大语言模型。"},
]
text = tokenizer.apply_chat_template(
    messages,
    tokenize=False,             # 只拼文本不转数字，方便查看模板实际长什么样
    add_generation_prompt=True, # 末尾追加生成提示符，告诉模型「该你回答了」
    enable_thinking=False,      # 思考模式开关：True 会输出 <think> 思考过程
)
model_inputs = tokenizer([text], return_tensors="pt").to(model.device)

# 生成文本
generated_ids = model.generate(
    **model_inputs,
    max_new_tokens=512  # 最多生成 512 个新 token；默认 32768 在 16GB 机器上极慢
)
output_ids = generated_ids[0][len(model_inputs.input_ids[0]):].tolist()

# 解析思考内容：</think> 的 token id 是 151668，之前是思考，之后是正文
try:
    index = len(output_ids) - output_ids[::-1].index(151668)
except ValueError:
    index = 0  # 没找到 </think>：模型没输出思考过程，全部按正文处理

thinking_content = tokenizer.decode(output_ids[:index], skip_special_tokens=True).strip("\n")
content = tokenizer.decode(output_ids[index:], skip_special_tokens=True).strip("\n")

print("thinking content:", thinking_content)
print("content:", content)
```

## 3 进一步阅读

- 每个参数的作用与关键概念 → [Qwen3 参数汇总](1.3_Qwen3%20参数汇总.md)
- 学习内容、建议顺序、部署选型 → [Qwen3 学习路线](1.4_Qwen3%20学习路线.md)
- 输出格式控制（Agent 场景）→ [输出格式控制](1.5_输出格式控制.md)
- 动手实验：[采样温度实验](实验/1.1_采样温度实验.md) / [对话控制实验](实验/1.2_对话控制实验.md) / [格式控制实验](实验/1.3_格式控制实验.md)
